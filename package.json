{
  "name": "cryptovoxels-collections",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ cryptovoxels-collections",
    "create-local": "graph create --node http://localhost:8020/ cryptovoxels-collections",
    "remove-local": "graph remove --node http://localhost:8020/ cryptovoxels-collections",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cryptovoxels-collections"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.1",
    "@graphprotocol/graph-ts": "^0.22.1"
  }
}
